<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.*?>

<VBox spacing="2" styleClass="sidebar-nav" xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.airline.reservation.ui.NavigationController">

    <!-- Logo/Header -->
    <HBox alignment="CENTER" style="-fx-padding: 20 15 25 15;">
        <Label text="✈️ AirLink" style="-fx-font-size: 20; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
    </HBox>

    <!-- Navigation Items -->
    <VBox spacing="0" styleClass="nav-items-container">
        <Button text="🏠 Dashboard" onAction="#handleDashboard" styleClass="nav-btn"/>
        <Button text="🎫 New Reservation" onAction="#handleNewReservation" styleClass="nav-btn nav-btn-primary"/>
        <Button text="✈️ Flights" onAction="#handleFlights" styleClass="nav-btn"/>
        <Button text="👥 Passengers" onAction="#handlePassengers" styleClass="nav-btn"/>
        <Button text="📋 Bookings" onAction="#handleBookings" styleClass="nav-btn"/>
        <Button text="📊 Reports" onAction="#handleReports" styleClass="nav-btn"/>
        <Button text="⚙️ Settings" onAction="#handleSettings" styleClass="nav-btn"/>
    </VBox>

    <!-- Spacer -->
    <Pane VBox.vgrow="ALWAYS"/>

    <!-- Footer -->
    <VBox spacing="8" style="-fx-padding: 15; -fx-border-color: #ecf0f1; -fx-border-width: 1 0 0 0;">
        <Label text="Admin User" style="-fx-font-weight: 600;"/>
        <Label text="Administrator" styleClass="small"/>
        <Button text="Logout" onAction="#handleLogout" styleClass="nav-btn-logout"/>
    </VBox>

</VBox>